services:
  - type: web
    name: ai-defense-suite-web-gui
    env: python
    plan: free
    buildCommand: pip install -r requirements-render.txt
    startCommand: gunicorn web_gui.app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
    healthCheckPath: /
